node_modules/
dist/
build/
.next/
coverage/
.angular/

# secrets — never commit
.env
.env.*
!.env.example

# logs
*.log

# OS
.DS_Store

# editors
.vscode/
.idea/

# Verifai runtime artifacts — ignore crawler output but keep directory structure
# Ignore everything inside each project's crawl folder (screenshots, scripts, raw data, logs)
backend/storage/projects/*/
backend/dist/
frontend/dist/
frontend/.angular/
